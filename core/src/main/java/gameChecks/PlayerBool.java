package gameChecks;

public class PlayerBool {
    public boolean playerbool(int playernumber)
    {
        if(playernumber == 0)
        {
            return true;
        }
        return false;
    }
}
